# Project Rules

- The database is self-hosted on a server and is **not** accessible locally. Do not attempt to connect to the database or run SQL queries directly on the local machine unless instructed otherwise. Provide SQL queries for the user to execute manually instead.
